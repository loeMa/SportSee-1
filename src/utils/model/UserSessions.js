
/**
 * Class to create new user average sessions
 */

export default class UserSessions{

    /**
     * 
     * @param {Number} userId 
     * @param {Number} sessions 
     * 
     */

    constructor(userId, sessions) {
        this.userId = userId;
        this.sessions = sessions
    }
}

